import 'package:flutter/material.dart';

class SolicitationItem {
  final String title;
  final Icon icon;
  final bool isActive;

  SolicitationItem({
    required this.title,
    required this.icon,
    required this.isActive,
  });
}
